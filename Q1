#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    string name, message;
    cout << "Enter the recipient's name: ";
    getline(cin, name);
    cout << "Enter a short message: ";
    getline(cin, message);
    ofstream outFile("greeting.txt");
    if (!outFile) {
        cerr << "Error opening file!" << endl;
        return 1;
    }
    outFile << "Dear " << name << ",\n";
    outFile << message << "\n";
    outFile << "Best Regards!" << endl;
    outFile.close();
    cout << "Greeting has been written to greeting.txt" << endl;
    return 0;
}
